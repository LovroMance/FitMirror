# FitMirror 项目状态

文档版本：v1.31  
最后更新：2026-04-07

本文档用于记录项目当前进度、阶段结论、阻塞项和交接信息。  
本文档不承载规范定义；涉及需求、功能、架构、数据库、UI 规范时，统一以 `docs/` 下对应规范文档为准。

## 一、文档职责

### 稳定基线文档

- `docs/开发指南.md`：长期开发规范、目录约束与交付要求，原则上只在开发规则变化时修改
- `docs/功能说明.md`：产品功能范围与功能边界，原则上只在功能范围变化时修改
- `docs/视觉风格与页面原型设计规范.md`：视觉语言与页面结构规范，原则上只在设计方向变化时修改
- `docs/需求分析.md`：产品目标、目标用户、核心场景与需求边界，原则上只在产品方向变化时修改

### 持续更新文档

- `docs/数据库架构.md`：当前数据存储事实、表结构与同步边界，数据结构变化时更新
- `docs/项目代码目录结构.md`：当前仓库目录事实与模块边界，结构变化时更新
- `docs/饮食管理.md`：饮食推荐与食物营养信息能力规划，饮食能力方案变化时更新
- `docs/项目状态.md`：阶段进度、结论、阻塞与交接信息，作为持续更新主文档
- `docs/文档索引.md`：文档职责、查阅顺序与维护约定，文档体系调整时更新

## 二、当前状态

- 项目当前处于移动端 PWA MVP 阶段
- 主流程已经形成可用闭环：注册 / 登录 -> 生成计划 -> 开始训练 -> 完成训练 -> 写入记录 -> 查看热图
- 文档体系已完成一轮收口，规范类内容已从进度类内容中分离
- “训练记录 -> 计划历史”的联动增强已完成首版落地，当前可从记录详情回看关联计划，并在计划历史页承接定位与使用痕迹
- 训练闭环承接体验已继续增强，记录页现在会对“刚完成训练”的结果提供顶部反馈、一键查看入口和详情高亮
- P0 主链路已完成一轮联调收口，计划同步、训练完成跳转、训练记录同步和记录页承接已验证通过
- 训练记录管理已继续补齐：当天详情可编辑/删除单条记录，并保留与计划历史的联动回看
- 饮食管理 P1 首版已落地：独立饮食页、静态知识检索、LLM 推荐生成和食物营养卡片已串通
- 饮食管理输入语义已从“忌口或限制”调整为“补充要求或问题”，当前既支持显式饮食限制，也支持自然语言补充需求
- 饮食推荐失败兜底已补上：上游模型异常时会自动重试，并回退到基于知识库的稳定推荐结果
- 饮食结果结构已继续增强：当前会对“补充要求或问题”输出显式回答区块，并把餐次建议拆成标题、食物、份量、原因和可替代项
- 主导航信息架构已调整为“首页 / 训练计划 / 饮食计划 / 个人资料”，并补上了一个简洁版个人资料页
- 个人资料页已继续补强：当前支持本地计划/记录概览、同步状态说明和基础偏好设置

## 三、当前已完成

- 账号体系：
  - 注册
  - 登录
  - 当前用户信息获取
  - 登录态恢复与路由守卫
- 训练计划：
  - 稳定通道生成
  - 流式通道生成
  - DeepSeek 失败时模板回退
  - 最近计划保存、恢复、删除
  - 当前计划轻量编辑（标题、总时长、动作顺序、删除动作）
  - 当前计划单动作替换（从动作库回填到编辑草稿）
  - 当前计划新增动作（从动作库追加到编辑草稿末尾）
  - 当前计划保存结果摘要（保存后反馈标题、总时长、动作替换/新增/删除/顺序变化）
  - 当前计划最近一次编辑结果卡片（计划页内保留最近一次保存摘要，支持再次回看）
  - 当前计划未保存变更提示（取消编辑、切页离开、关闭页面前提醒）
  - 计划历史列表独立页面
  - 计划云同步
- 训练闭环：
  - 从计划页进入训练会话
  - 按动作顺序推进训练
  - 完成训练后自动写入训练记录
- 训练记录：
  - 最近六周热图
  - 周/月切换
  - 训练统计
  - 趋势摘要
  - 当天详情增强展示
  - 单条训练记录编辑（时长、完成状态）
  - 单条训练记录删除
  - 记录详情关联计划摘要与回看入口
  - 手动记录 / 原计划已删除 / 可回看计划 三类详情状态区分
- 计划历史增强：
  - 按训练记录传入的 `planId` 自动展开并定位目标计划
  - 显示计划是否已用于训练及最近使用标识
  - 按“最近使用优先，其次创建时间倒序”排序历史计划
  - 支持“全部计划 / 已用于训练 / 未用于训练”前端筛选
- 动作库：
  - 静态动作数据读取
  - 关键词、部位、难度、器械筛选
  - 动作详情弹层
  - 计划页联动预筛选
  - 收藏与最近查看
- 饮食管理：
  - 独立饮食推荐页
  - 基于静态知识库的规则检索
  - LLM 生成 1 天饮食建议
  - 推荐结果内食物营养卡片展示
  - 首页饮食管理入口
  - 支持“补充要求或问题”输入，并对显式忌口继续做过滤处理
  - 上游 LLM 失败时自动重试一次，并可回退到稳定推荐结果
  - 对“补充要求或问题”输出显式回答区块
  - 餐次结果升级为结构化字段（标题、推荐食物、建议份量、搭配原因、可替代食物）
  - 已补充目标差异、工作日省时、外卖场景、乳糖不耐受和不吃辣等知识条目
- 页面结构：
  - 首页已改为更轻的分区式布局，减少大卡片堆叠
  - 底部主导航已抽为通用组件，并接入首页、训练计划、饮食计划、个人资料页
  - 个人资料页已补到第二版，可承接账号信息、本地概览、偏好设置、训练记录、历史计划、饮食计划和退出登录入口
  - 训练记录页已接入统一主导航，主入口页之间的切换路径更一致

## 四、当前技术边界

- 后端当前承接账号体系、计划生成和本地数据同步
- 训练计划与训练记录事实源当前仍以前端 Dexie 为先，并同步到后端 MySQL
- 后端数据库当前包含账号、计划、训练记录模型
- 当前不把桌面端作为正式适配目标

## 五、交接约定

- 规范类判断优先看：
  - `docs/文档索引.md`
  - `docs/需求分析.md`
  - `docs/功能说明.md`
  - `docs/开发指南.md`
  - `docs/视觉风格与页面原型设计规范.md`
  - `docs/数据库架构.md`
  - `docs/项目代码目录结构.md`
- 进度恢复、阶段结论、下一步判断优先看本文档
- 若代码与文档冲突，先以代码为准，再回写文档

## 六、下次继续时建议先做的事

1. 使用真实登录账号进入 `训练记录` 页，补做一轮可视化验收，重点确认 `cal-heatmap` 的月翻页、周一到周日对齐与点击日期打开详情。
2. 若需要进一步增强可读性，可评估补充工作日标签（周一到周日）和当日 Tooltip 文案。
3. 结合后续反馈评估是否将首页热图也统一到同一套 `cal-heatmap` 渲染能力。

## 七、本次更新记录

- 已将训练记录页热图从手写网格/旧方案重构为 `cal-heatmap`，新增组件 `frontend/src/views/workout/components/WorkoutCalendarHeatmap.vue`。
- 已将热图范围改为“自然月分页”：支持上月/下月切换，默认当前月；从训练完成页跳入时会自动定位到完成日期所在月份。
- 已在 `useWorkoutLog` 和 `workout-heatmap` 中完成配套重构：按自然月生成日期范围，输出月级标题与区间，保留原有训练统计和当天详情联动。
- 已更新热图图例为 0-4 强度档，并移除前端依赖 `vue-calendar-heatmap`。
- 本次验证：`npm.cmd --prefix frontend run lint` 通过；`npm.cmd --prefix frontend run test -- workout-heatmap.test.ts useWorkoutLog.test.ts` 通过；`npm.cmd --prefix frontend run build` 通过。已尝试使用 Chrome DevTools 做页面验证，但当前环境未完成登录态准备，`/workout-log` 仅呈现空白壳层，暂无法完成热图交互的最终可视化确认。

## 八、下一步建议

1. 先补一轮“已登录 + 有训练数据”的浏览器验收，并记录截图（至少包含一个有训练数据月份和一个无训练数据月份）。
2. 如验收通过，再考虑为热图补充悬浮提示（日期/次数/时长）以增强可解释性。
3. 若后续继续统一视觉，可将主页热图也迁移为同一组件，减少两套热图渲染维护成本。
