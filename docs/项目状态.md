# FitMirror 项目状态

文档版本：v1.9  
最后更新：2026-04-02

本文档用于记录项目当前进度、阶段结论、阻塞项和交接信息。  
本文档不承载规范定义；涉及需求、功能、架构、数据库、UI 规范时，统一以 `docs/` 下对应规范文档为准。

## 一、文档职责

### 稳定基线文档

- `docs/开发指南.md`：长期开发规范、目录约束与交付要求，原则上只在开发规则变化时修改
- `docs/功能说明.md`：产品功能范围与功能边界，原则上只在功能范围变化时修改
- `docs/视觉风格与页面原型设计规范.md`：视觉语言与页面结构规范，原则上只在设计方向变化时修改
- `docs/需求分析.md`：产品目标、目标用户、核心场景与需求边界，原则上只在产品方向变化时修改

### 持续更新文档

- `docs/数据库架构.md`：当前数据存储事实、表结构与同步边界，数据结构变化时更新
- `docs/项目代码目录结构.md`：当前仓库目录事实与模块边界，结构变化时更新
- `docs/项目状态.md`：阶段进度、结论、阻塞与交接信息，作为持续更新主文档
- `docs/文档索引.md`：文档职责、查阅顺序与维护约定，文档体系调整时更新

## 二、当前状态

- 项目当前处于移动端 PWA MVP 阶段
- 主流程已经形成可用闭环：注册 / 登录 -> 生成计划 -> 开始训练 -> 完成训练 -> 写入记录 -> 查看热图
- 文档体系已完成一轮收口，规范类内容已从进度类内容中分离
- “训练记录 -> 计划历史”的联动增强已完成首版落地，当前可从记录详情回看关联计划，并在计划历史页承接定位与使用痕迹

## 三、当前已完成

- 账号体系：
  - 注册
  - 登录
  - 当前用户信息获取
  - 登录态恢复与路由守卫
- 训练计划：
  - 稳定通道生成
  - 流式通道生成
  - DeepSeek 失败时模板回退
  - 最近计划保存、恢复、删除
  - 计划历史列表独立页面
  - 计划云同步
- 训练闭环：
  - 从计划页进入训练会话
  - 按动作顺序推进训练
  - 完成训练后自动写入训练记录
- 训练记录：
  - 最近六周热图
  - 周/月切换
  - 训练统计
  - 趋势摘要
  - 当天详情增强展示
  - 记录详情关联计划摘要与回看入口
  - 手动记录 / 原计划已删除 / 可回看计划 三类详情状态区分
  - 手动补录入口
- 计划历史增强：
  - 按训练记录传入的 `planId` 自动展开并定位目标计划
  - 显示计划是否已用于训练及最近使用标识
  - 按“最近使用优先，其次创建时间倒序”排序历史计划
  - 支持“全部计划 / 已用于训练 / 未用于训练”前端筛选
- 动作库：
  - 静态动作数据读取
  - 关键词、部位、难度、器械筛选
  - 动作详情弹层
  - 计划页联动预筛选
  - 收藏与最近查看

## 四、当前技术边界

- 后端当前承接账号体系、计划生成和本地数据同步
- 训练计划与训练记录事实源当前仍以前端 Dexie 为先，并同步到后端 MySQL
- 后端数据库当前包含账号、计划、训练记录模型
- 当前不把桌面端作为正式适配目标

## 五、交接约定

- 规范类判断优先看：
  - `docs/文档索引.md`
  - `docs/需求分析.md`
  - `docs/功能说明.md`
  - `docs/开发指南.md`
  - `docs/视觉风格与页面原型设计规范.md`
  - `docs/数据库架构.md`
  - `docs/项目代码目录结构.md`
- 进度恢复、阶段结论、下一步判断优先看本文档
- 若代码与文档冲突，先以代码为准，再回写文档

## 六、下次继续时建议先做的事

1. 确认这轮之后是否需要补 `README.md` 收口
2. 评估是否进入计划历史交互细节增强
3. 评估是否进入同步冲突处理与可见性增强
4. 每次结构、数据或阶段状态变化后，同步更新对应文档

## 七、本次更新记录

- 已完成训练记录页边界体验增强，补齐手动记录、可回看计划、原计划已删除三类详情状态的文案与行为区分
- 已将记录详情中的边界文案整理为前端纯逻辑映射，避免在页面模板中堆叠多分支判断
- 已补充训练记录详情边界文案测试，覆盖计划记录、手动记录和计划缺失三类场景
- 已完成前端 lint、前端测试、后端测试回归，当前结果正常
- 已使用 Chrome DevTools MCP 对训练记录页进行页面验证，确认当天详情弹层能正确展示三类记录状态，且只有可回看计划的记录会出现“查看对应计划”入口
- 已更新项目级 `AGENTS.md`，新增“每次修改完成并验证通过后都必须进行一次 git commit”的执行要求，并将其纳入固定任务节奏
- 已完成计划历史页“顺手度增强”首版实现，新增最近使用优先排序、已使用/未使用筛选，以及更清晰的使用状态文案层级
- 已保证历史计划页在带 `planId` 承接进入时优先展示目标计划；当当前筛选不包含目标计划时，会自动切换到可见该计划的筛选状态
- 已补充计划历史页前端测试，覆盖排序、筛选和路由目标计划可见性
- 已完成前端 lint、前端测试、后端测试回归，当前结果正常
- 已使用 Chrome DevTools MCP 对计划历史页进行页面验证，确认“目标计划默认置顶并展开”“筛选切换后列表与详情联动正常”“未使用计划单独筛出”三项行为符合预期
- Chrome DevTools MCP 实测中出现 `http://127.0.0.1:3000/api/auth/me` 与 `/plans/sync` 的 `ERR_CONNECTION_REFUSED`，属于本地未启动后端时的环境报错，不是本次前端改动新增的界面逻辑错误
- 已更新 `docs/开发指南.md`，补充 YAGNI、DRY、KISS、SOLID 四点开发原则，并明确“前端可验证改动完成后需调用 Chrome DevTools MCP 服务进行页面检查”的验证要求
- 已更新项目级 `AGENTS.md`，把 Chrome DevTools MCP 验证要求和四点开发原则固化为任务执行规则
- 已完成“计划与训练记录体验增强”首版落地，覆盖训练记录详情增强、记录到计划历史页的联动跳转，以及计划历史页对训练使用痕迹的承接
- 已将训练记录当天详情从基础统计扩展为可读条目列表，补充完成状态、时长、来源类型、关联计划标题/目标摘要和“查看对应计划”入口
- 已打通 `/records -> /plans/history?planId=...` 的页面承接链路，支持历史计划页自动展开、滚动定位并高亮目标计划
- 已在计划历史页增加本地推导的训练使用标识，支持展示“最近使用”与训练次数信息
- 已补充训练记录详情映射、计划历史承接、训练完成后保留 `planId` 的前端测试
- 已完成前端 lint、前端测试、后端测试回归，当前结果正常
- 已将项目级 `AGENTS.md` 改为正式规则文本，不再使用注释包裹的占位写法
- 已把“修改前阅读 `docs/开发指南.md`、修改前后查看并更新 `docs/项目状态.md`”固化为项目级工作规则
- 已把 `docs/开发指南.md`、`docs/功能说明.md`、`docs/视觉风格与页面原型设计规范.md`、`docs/需求分析.md` 纳入“无特殊情况不修改”的稳定基线文档约束
- 已对齐 `docs/文档索引.md` 与 `docs/项目状态.md` 对“持续更新文档”的口径
- 已修正 `docs/功能说明.md` 中对“基础偏好设置”能力的过度表述，使其与当前实际可见功能一致
- 已修正 `docs/视觉风格与页面原型设计规范.md` 中训练记录页模块与热图强度梯度描述，使其与当前页面实现一致
- 已调整 `README.md` 中的文档入口顺序，使其更接近 `docs/文档索引.md` 的查阅顺序

## 八、下一步建议

1. 后续每次进入新任务时，先按 `AGENTS.md` 执行一轮“开发指南 -> 项目状态 -> 实施 -> 项目状态更新”的固定节奏
2. 后续每次任务完成后，除更新 `docs/项目状态.md` 外，还应补一次准确的 git commit，避免工作区长期堆积未提交变更
3. 后续凡是涉及前端页面、交互或主链路的更新，收尾时都应补一轮 Chrome DevTools MCP 实测，不只依赖单测或 lint
4. 下一步可优先补训练记录页与计划历史页之间的端到端浏览器主链路验证，重点确认从真实训练完成到记录详情、再回到历史计划页的完整闭环
5. 若继续增强计划历史页，可考虑补轻量搜索或按目标类型筛选，但仍以不新增后端接口为前提
